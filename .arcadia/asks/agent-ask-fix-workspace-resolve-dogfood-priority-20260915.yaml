{"agent_ask":"v1","request_id":"fix-workspace-resolve-dogfood-priority-20260915","project":"arcadia","intent":"log","desired_result":"Record that resolveWorkspace() silently let a stub .arcadia-workspace dogfood marker outrank the configured defaultWorkspace with no warning, discovered when an Agent Ask nearly settled against a fresh disconnected workspace instead of martianrover; fixed and opened as PR #263.","rationale":"Stop-the-line defect: a wrong-workspace settle would have silently discarded real project state with no error. Fix reorders resolution (direct marker > user config default > dogfood marker), adds a warning when the dogfood fallback is used, and honors ARCADIA_INVOKED_FROM. Not tied to an existing plan Action, so recording as a Log entry with the PR link rather than inventing one."}
