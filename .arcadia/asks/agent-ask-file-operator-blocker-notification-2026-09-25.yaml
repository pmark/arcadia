{"agent_ask": "v1", "request_id": "file-operator-blocker-notification-2026-09-25", "project": "arcadia", "intent": "action", "requested_authority": "propose", "desired_result": "File a first-class operator notification for agent-reported blockers, so a CI blocker reaches Discord with its full detail rather than only an Agent Ask request id.", "rationale": "Operator instruction 2026-09-25: CI failures are fixed immediately in every session, and a blocker is reported on the default notification channel (usually Discord). docs/agents-context.md now requires that, using the only existing agent-reachable path: a settled log Agent Ask, which the Discord poller relays as 'Appended one Project Log entry for Agent Ask <request_id>' without the blocker text. This Action gives the notification its content. It is held behind prove-two-action-unattended-production per the same day's critical-path instruction, because the interim path already delivers a notification.", "actions": [{"id": "notify-operator-on-agent-blockers", "desired_result": "Let any agent session record an operator notification that the default channel relays with its full text.", "acceptance": ["A verb command (for example arcadia operator notify) records a durable operator notification with project, severity, title, body, and an optional PR or Action reference; it is idempotent on a caller-supplied key and makes no model call.", "The Discord notification poller relays each new notification once, with its title, body and reference, through the existing CLI-snapshot boundary; the dashboard /runs Alerts panel lists open notifications.", "It works from any Project repository and any coding-agent provider, including a managed-production Session; where no workspace resolves, the command fails with a named remedy rather than silently succeeding.", "docs/agents-context.md's 'CI failures are fixed immediately' step 4 is updated to use the command instead of the log-Ask interim, and AGENTS.md is regenerated.", "Deterministic tests cover recording, idempotent replay, Discord relay of the full text exactly once, and the no-workspace refusal; pnpm test and the core, Discord and Dashboard builds pass."], "dependencies": ["prove-two-action-unattended-production"], "references": ["docs/agents-context.md", "apps/discord-bot/src/notifications/poller.ts", "src/ask/settlement.ts"]}]}
