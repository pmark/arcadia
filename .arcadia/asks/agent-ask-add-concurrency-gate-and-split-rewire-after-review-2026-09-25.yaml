{"agent_ask":"v1","request_id":"add-concurrency-gate-and-split-rewire-after-review-2026-09-25","project":"arcadia","intent":"action","desired_result":"Add the two Actions the adversarial review found must land before ready-set admission: a per-admission concurrency gate and a split that keeps dependents blocked until its remainder is done.","rationale":"The adversarial review (docs/reviews/2026-09-25-ready-set-admission-adversarial-review.md) found that the concurrency gate (admit-ready-set-across-repositories AC6) holds back maxConcurrentSessions > 1 but not AC2/AC3, which retire the settlement-advanced current_action on merge, the exact mechanism the operator wanted proven sequentially first (F1). AC3 names Decision 0070 host settler as sole writer, but that settler is held behind prove-two-action-unattended-production, so B2 cannot satisfy AC3 without bypassing that hold or stalling pointer-driven dispatch (F2). The gate sits in a pure validation function that activateProduction and issueAdmission never call, is checked once, and keys on the active Plan (F5); split marks a narrowed Action done, opening the gate and releasing dependents early (F4); B1 names only canonicalOrder while collectUnmetDependencies and the parser also treat unknown ids as satisfied or dangling (F6, F7); prove-concurrent-ready-set-admission cannot run against a gate with no exception and cannot observe a pointer race across two repositories (F3); touches: absent-vs-empty can invert the pipelining safe default (F10); a stalled Session silently holds a portfolio-wide host slot (F9). Filed as intent action with target_ref amendments, not intent plan, so the queue is not reflowed.","actions":[{"id":"enforce-concurrency-gate-at-admission","desired_result":"Cap production concurrency at one Session on every admission until both concurrency proofs are done, with an expiring operator-granted rehearsal exception as the only way around it.","acceptance":["issueAdmission (src/production/policy.ts) caps effective concurrency at 1 on every admission, whatever maxConcurrentSessions the stored scope carries, unless plan/bootstrap-managed-production-to-build-flight-deck#prove-two-action-unattended-production and plan/bootstrap-managed-production-to-build-flight-deck#prove-concurrent-ready-set-admission are both done on the base branch; a refused admission names both ids.","The check runs on every admission and names both Actions by Plan-qualified id, so reopening either Action restores the cap without deactivating the policy, and a later change of active Plan neither lifts nor permanently locks the gate.","production preview and activate with --concurrency greater than 1 report the effective cap and its reason while the gate is closed, instead of silently recording a limit that will not be honoured.","The only way to exceed the cap before both proofs are done is an explicit, expiring rehearsal exception on the operator-granted policy scope that names prove-concurrent-ready-set-admission; it lapses at its expiry or on deactivation, and nothing else lifts the cap.","Deterministic tests cover: the cap holding for a stored scope above 1 while the gate is closed, including a scope written directly without passing through activation; the cap lifting once both Actions are done; the cap returning when one is reopened; and the rehearsal exception being honoured only before its expiry; pnpm test and the core, Discord and Dashboard builds pass."],"dependencies":[],"references":["src/production/policy.ts","src/production/activation.ts","src/commands/production.ts","docs/reviews/2026-09-25-ready-set-admission-adversarial-review.md"]},{"id":"rewire-dependents-on-split","desired_result":"A split no longer satisfies anything that depended on the narrowed Action until its remainder Actions are done too.","acceptance":["When a split settles (src/ask/settlement.ts), every Action whose depends_on names the split Action also gains the remainder Action ids, so no dependent becomes ready while any remainder is still open.","Any readiness or gate check that requires a named Action to be done, including enforce-concurrency-gate-at-admission, also requires every remainder Action split from it to be done.","Deterministic tests cover: a dependent that stays blocked after a split until its remainder is done, and the concurrency gate staying closed when one of its proof Actions is split with an open remainder; pnpm test and the core, Discord and Dashboard builds pass."],"dependencies":[],"references":["src/ask/settlement.ts","src/docs/dispatch.ts","docs/reviews/2026-09-25-ready-set-admission-adversarial-review.md"]}]}
