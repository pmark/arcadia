{"agent_ask":"v1","request_id":"independent-review-rubric-digest-2026-09-24","project":"arcadia","intent":"plan","target_ref":"plan/create-one-inactive-plan-for-on-demand-evidence-based-adversarial-review-of-a-se","desired_result":"Key pre-PR review receipt reuse on the rubric's content digest, so an edited rubric never reuses a verdict given under different instructions.","rationale":"CodeRabbit finding on PR #597: a rubric id is not immutable, so a changed rubric could reuse a receipt produced under different review instructions.","actions":[{"target_ref":"action/request-pre-pr-code-review","desired_result":"Add arcadia qa code-review, which packets the current worktree's commits, runs the selected reviewer against the code-review rubric, persists a receipt pinned to the reviewed packet, and returns done, fix, or cap in the same shape as arcadia pr code-review, counting fix rounds per branch.","acceptance":["arcadia qa code-review --json returns done, fix with findings, or cap after three fix rounds on the same branch, using the verdict fields arcadia pr code-review returns.","Re-running reuses the persisted receipt only when head SHA, base SHA, rubric id, and rubric content digest all match the current packet; any difference runs a new review.","The receipt records head and base SHAs, rubric id and content digest, reviewer provider, model and session, and every finding.","A deterministic test drives all three verdicts end to end with a stubbed reviewer, and tests prove that neither a moved base at an unchanged head nor an edited rubric under an unchanged id reuses the receipt."],"dependencies":["review-packet-for-local-commits","select-review-provider-with-fallback"],"references":["src/stewardship/codeRabbitReview.ts","src/commands/prCodeReview.ts","src/commands/qa.ts"]}]}
