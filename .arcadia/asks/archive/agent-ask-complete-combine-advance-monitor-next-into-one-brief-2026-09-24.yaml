{
  "agent_ask": "v1",
  "request_id": "complete-combine-advance-monitor-next-into-one-brief-2026-09-24",
  "project": "arcadia",
  "intent": "complete",
  "target_ref": "action/combine-advance-monitor-next-into-one-brief",
  "candidate_revision": "a7f949da3625bc82c0bcb57bedf2918a9605f8fe",
  "desired_result": "Mark combine-advance-monitor-next-into-one-brief complete.",
  "evidence": [
    {
      "criterion": "A new broker operation (extending scripts/arcadia-go-broker.ts and src/goBroker.ts) runs the existing advance logic, the existing work-monitor preflight, and the existing next dispatch resolution in one process invocation from the prepared worktree, and returns one combined JSON response including the exact rendered dispatch-brief text the operator must see -- no separate `pnpm arcadia next` invocation is needed to produce that text.",
      "status": "met",
      "note": "src/goBroker.ts gains a `brief` operation that calls the existing runAdvanceCommand, runWorkMonitorCommand, and runNextCommand once each and returns their data plus data.dispatchBrief (renderNextSuccess joined). Live-run proof against this real prepared worktree in docs/reports/combine-advance-monitor-next-into-one-brief-qa.md shows dispatchBrief byte-identical to a standalone `pnpm arcadia next` run."
    },
    {
      "criterion": "The existing standalone advance, work-monitor, and next commands are unchanged and keep working exactly as they do today for any other caller; this adds one new combined entry point rather than removing or altering the individual ones.",
      "status": "met",
      "note": "The existing advance/work-monitor branches in runGoBroker are untouched; src/commands/next.ts is untouched entirely. tests/go-broker.test.ts's pre-existing advance/work-monitor tests pass unmodified, proving the same call shapes still work."
    },
    {
      "criterion": "A failure at any one of the three stages (for example: dispatch not resolvable, or work-monitor finding a preservation blocker) is reported with the same field-level specificity the standalone command would give for that stage, not swallowed or genericized by the combination.",
      "status": "met",
      "note": "runBriefStage() re-throws the original ArcadiaError's exact code/message/details, only adding a `stage` tag. tests/go-broker.test.ts covers a refusal injected at the advance, work-monitor, and next (project-slug resolution) stages, asserting the original message/details survive verbatim plus the stage tag, and that later stages never run."
    },
    {
      "criterion": "No model or AI call is introduced anywhere in the combined path; it remains exactly as deterministic as the three calls it replaces.",
      "status": "met",
      "note": "The combined path is three existing deterministic read functions (resolveProjectTransition, scanProjectWorkingCopies, resolveDispatch) plus one deterministic discoverDocs lookup for the Project slug; no LLM or network call is introduced."
    },
    {
      "criterion": "arcadia-go.SKILL.md's step 3 is rewritten to issue exactly one launcher call from the prepared worktree and paste its returned brief verbatim as the opening chat message, replacing the current three-call sequence (advance broker, work-monitor broker, pnpm arcadia next); step 4's standalone work-monitor guidance is removed or marked redundant accordingly.",
      "status": "met",
      "note": "src/agentSetup/arcadia-go.SKILL.md's numbered workflow now has the prepared-worktree step (renumbered step 2, since old step 1's go handoff stays step 1) issue only the brief launcher and paste data.dispatchBrief verbatim; the standalone advance/work-monitor launchers moved out of the numbered workflow into a new 'Standalone launchers' reference section explicitly marked 'never invoke these as part of the numbered workflow above'."
    },
    {
      "criterion": "Deterministic tests cover: the combined success path returns all three results including the rendered brief text; a failure injected at each of the three stages individually is reported with that stage's exact failure; and the standalone advance/work-monitor/next commands are proven unaffected by the change.",
      "status": "met",
      "note": "tests/go-broker.test.ts's new 'the combined brief operation' describe block covers the success path (asserting result.data equals advance/workMonitor/next data plus the rendered dispatchBrief) and a failure injected at each of the advance, work-monitor, and next/project-slug stages; the pre-existing advance-only and work-monitor-only tests, plus every go-broker-agent-setup.test.ts case, pass unmodified in shape, proving the standalone paths unaffected. Full run: tests/go-broker.test.ts + tests/go-broker-agent-setup.test.ts + tests/production-fault-matrix-runtime.test.ts = 51 passed, 0 failed."
    },
    {
      "criterion": "Preserve a runnable operator QA artifact showing the exact before/after command and round-trip count from a prepared worktree.",
      "status": "met",
      "note": "docs/reports/combine-advance-monitor-next-into-one-brief-qa.md states the before (3 round trips: advance broker, work-monitor broker, pnpm arcadia next --project <slug>) and after (1 round trip: the brief launcher) commands, plus a live run of `mise exec -- node --import tsx scripts/arcadia-go-broker.ts claude brief` against this real prepared worktree, whose output is recorded in the report."
    }
  ]
}
