{
  "agent_ask": "v1",
  "request_id": "promote-issue-284-go-transport-readiness-2026-09-17",
  "project": "arcadia",
  "intent": "action",
  "requested_authority": "propose",
  "desired_result": "The protected broker's agent go transport reports ready only when a worker can actually service a go request, and a timed-out or refused request clears its pending marker so a retry needs no hand-editing.",
  "rationale": "Issue #284: on this host `arcadia go-broker status` reported `Agent go transport: READY` while three opencode go requests failed — one immediately, two after the five-minute budget — and a timed-out request stranded `.arcadia-go-request` so every later request failed EEXIST. The worker was alive and ticking managed production, so a fresh heartbeat is currently indistinct from a worker that can service go. This blocks every `arcadia go` continuation, including the opencode handoff the credit-exhausted host depends on, and it blocks its own repair path. Promote it ahead of the pointed Action.",
  "actions": [
    {
      "id": "fix-agent-go-transport-readiness",
      "desired_result": "The protected broker's agent go transport reports ready only when a worker can actually service a go request, and a timed-out or refused request clears its pending marker so a retry needs no hand-editing.",
      "acceptance": [
        "`arcadia go-broker status` reports the agent go transport NOT READY unless a worker able to service a go request is registered, so a merely fresh heartbeat from a worker whose tick is stuck in managed production can no longer satisfy it.",
        "A go request timeout or refusal removes the pending `.arcadia-go-request` marker, matching the contract #272 set for the preserve path, so an immediate retry succeeds without removing the file by hand.",
        "Deterministic tests cover readiness false while the worker cannot service a request, marker cleanup on timeout and on refusal, and a successful request still returning its host response; the existing preservation transport and managed-production tick behavior is unchanged.",
        "START_HERE.md and docs/COMMANDS.md state that a fresh heartbeat alone is not sufficient for the go transport and name the recovery for a stranded request marker.",
        "pnpm test and the core, Discord, and Dashboard builds pass; no new approval, capacity, or paid-fallback authority is introduced."
      ],
      "dependencies": [],
      "references": [
        "https://github.com/pmark/arcadia/issues/284",
        "src/sessions/preservationTransport.ts",
        "src/goBroker.ts",
        "src/commands/goBrokerInstall.ts",
        "START_HERE.md",
        "docs/COMMANDS.md"
      ]
    }
  ]
}
