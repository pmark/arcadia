{"agent_ask": "v1", "request_id": "complete-surface-batch-readiness-view-2026-09-22", "project": "arcadia", "intent": "complete", "target_ref": "action/surface-batch-readiness-view", "candidate_revision": "fe77dc4da2e93c2b3b31ff4271f10be16daafc68", "evidence": [{"criterion": "resolveReadySet (or a thin wrapper over it) groups its existing ready-set output into lanes by repo/Project -- same-repo Actions in one lane labeled sequence-advised, different-repo Actions each in their own lane -- and sums each lanes token_impact tier plus an overall total, with zero model calls.", "status": "met"}, {"criterion": "The same function computes the batch boundary: walk the dependency-clear ready set forward from current_action and stop at the first Decision, deferred Action, clarification: question_open Action, or capacity-gated proof run; everything before the boundary is the batch, everything after is named but not expanded.", "status": "met"}, {"criterion": "apps/dashboard/app/runs/page.tsx and production-control-panel.tsx extend the existing Next up section into This push (the batch, expanded, lanes visible) and Next push (collapsed by default, same pattern as Recent history) instead of the current flat list; the boundary itself renders as an actionable prompt (a Decision link, an un-defer control, or a plain named blocker) rather than prose.", "status": "met"}, {"criterion": "This reuses the existing /api/production-control route and useProductionControl hook -- extend their payload/types, do not add a new API route or a new top-level dashboard page.", "status": "met"}, {"criterion": "The GitHub board mirror writes the computed lane/batch position through the existing card-status projection pipeline (the same mechanism Gate 1 already uses to keep Arcadia status fresh per card), recomputed on the same cadence as that projection -- never cached separately, since a stale batch label on a card is worse than none.", "status": "met"}, {"criterion": "The mirror does not attempt to create or manage a GitHub Projects saved view, grouped board, or swimlane -- GitHubs API has no capability for that (confirmed, docs/managed-production-readiness.md Gate 1). Document that creating a grouped view from the mirrored field is a one-time manual operator step in GitHubs own UI, not something this Action builds.", "status": "met"}, {"criterion": "A regression test covers lane grouping (same-repo vs cross-repo), the boundary computation stopping at each of the four named gate types, and token rollup arithmetic, using a fixture Plan rather than live production state.", "status": "met"}], "desired_result": "Mark surface-batch-readiness-view complete: the Runs dashboard shows the current push as lanes with a token rollup, the batch boundary at the first operator gate, and a collapsed next push, and the same computation is projected onto GitHub board cards through the existing status pipeline.", "rationale": "Implemented in this candidate worktree. resolveBatch (src/docs/batch.ts) wraps resolveReadySet, which now exposes the ordered walk it already computed; lanes group by repository and are labeled sequence-advised when a lane holds more than one Action; token tiers roll up per lane and overall with zero model calls. The boundary stops at each of the four gates and is covered by tests/batch.test.ts. The /runs panel renders This push and Next push, reusing /api/production-control and useProductionControl. Arcardia push is written by the same projectScheduleToBoard pass that writes statuses, and a stale label forces a re-projection; the field is optional and only `schedule github link` creates it. Docs updated in github-board-guide.md, production-scheduling.md, and START_HERE.md. Full suite 1938 passed / 13 skipped, core+discord+build clean, dashboard production build clean, and a new Playwright spec passes against a real workspace."}
