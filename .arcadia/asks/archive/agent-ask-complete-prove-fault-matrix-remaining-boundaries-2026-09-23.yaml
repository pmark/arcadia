{
  "agent_ask": "v1",
  "request_id": "complete-prove-fault-matrix-remaining-boundaries-2026-09-23",
  "project": "arcadia",
  "intent": "complete",
  "target_ref": "action/prove-fault-matrix-remaining-boundaries",
  "candidate_revision": "da3f9cb093df5662293581ac3f02db83dc3e205b",
  "desired_result": "Prove the completion/pointer, process-health, and runtime contract-20 boundaries and close the missing-artifact quality gate.",
  "rationale": "Three new seeded fault-matrix test files (tests/production-fault-matrix-completion.test.ts, tests/production-fault-matrix-process-health.test.ts, tests/production-fault-matrix-runtime.test.ts) each run 100 reproducible seeded interleavings with real crash injection and zero invariant violations, including a real gap found and fixed (a stale-pointer race between two concurrent completions, now covered by resolveDispatch's own safety net). settleAgentAsk now refuses a completion whose declared expected_artifact resolves to a real, missing repo-relative path, with tests covering the refusal, the present-path success case, and that prose expected_artifact is never checked. docs/evidence/managed-production-release-evidence-index.md is updated: Stage 1 is now fully proven.",
  "evidence": [
    { "criterion": "The completion/pointer, process-health, and runtime race scenarios from the contract-20 boundary table each run at least 100 reproducible seeded interleavings with crash injection points specified in the harness, with zero invariant violations and retained failing-seed/timeline evidence for any violation found and fixed.", "status": "met", "note": "tests/production-fault-matrix-completion.test.ts, tests/production-fault-matrix-process-health.test.ts, and tests/production-fault-matrix-runtime.test.ts each run 100 seeds with crash injection (AgentAskSettlementTestHooks points, a flag/event transaction crash, and 7 run()-step failures respectively); all pass with zero violations at commit 32b20d7d. The evidence-writing helper (violation-<scenario>-<seed>.json / summary-<scenario>.json) is wired identically to the existing four scenarios and is exercised whenever ARCADIA_FAULT_MATRIX_EVIDENCE_DIR is set. One real violation was found mid-implementation (a concurrent-completion pointer race) and fixed by verifying resolveDispatch's documented safety net instead of a false absolute-freshness guarantee; see the completion-pointer row's own file comment." },
    { "criterion": "A completion whose declared Artifact is absent is refused, with a test.", "status": "met", "note": "src/ask/settlement.ts now refuses a `complete` settlement whose expected_artifact resolves to a real repo-relative path that does not exist. tests/agent-ask-complete.test.ts covers the refusal, the present-path success case, and that prose expected_artifact (the common case) is never checked; a pre-existing docs/contract.md vs contract.md fixture mismatch in tests/session-reconciliation.test.ts that this check surfaced was also fixed." },
    { "criterion": "docs/evidence/managed-production-release-evidence-index.md is updated with each new row's status, revision, and reproduction procedure.", "status": "met", "note": "The Completion and pointer, Process health, and Runtime rows, and the Missing artifact quality-gate row, are updated to pass with seed/violation counts, evidence file references, and coverage notes; the top revision line and reproduction command (now a glob over all four production-fault-matrix*.test.ts files) are updated to commit 32b20d7d." }
  ],
  "requested_authority": "apply_if_approved"
}
