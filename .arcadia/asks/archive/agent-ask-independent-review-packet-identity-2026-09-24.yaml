{"agent_ask":"v1","request_id":"independent-review-packet-identity-2026-09-24","project":"arcadia","intent":"plan","target_ref":"plan/create-one-inactive-plan-for-on-demand-evidence-based-adversarial-review-of-a-se","desired_result":"Reuse a pre-PR review receipt only when the whole review packet is unchanged, not merely the head commit.","rationale":"CodeRabbit finding on PR #597: an unchanged head can yield a different merge base and patch once the base branch moves, so reuse keyed on head alone could return a verdict for evidence other than the current packet.","actions":[{"target_ref":"action/request-pre-pr-code-review","desired_result":"Add arcadia qa code-review, which packets the current worktree's commits, runs the selected reviewer against the code-review rubric, persists a receipt pinned to the reviewed packet, and returns done, fix, or cap in the same shape as arcadia pr code-review, counting fix rounds per branch.","acceptance":["arcadia qa code-review --json returns done, fix with findings, or cap after three fix rounds on the same branch, using the verdict fields arcadia pr code-review returns.","Re-running reuses the persisted receipt only when head SHA, base SHA, and rubric id all match the current packet; any difference runs a new review.","The receipt records head and base SHAs, rubric id, reviewer provider, model and session, and every finding.","A deterministic test drives all three verdicts end to end with a stubbed reviewer, and a test proves a moved base at an unchanged head does not reuse the receipt."],"dependencies":["review-packet-for-local-commits","select-review-provider-with-fallback"],"references":["src/stewardship/codeRabbitReview.ts","src/commands/prCodeReview.ts","src/commands/qa.ts"]}]}
