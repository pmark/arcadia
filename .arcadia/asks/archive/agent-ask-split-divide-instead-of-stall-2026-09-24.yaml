{"agent_ask":"v1","request_id":"split-divide-instead-of-stall-2026-09-24","project":"arcadia","intent":"split","target_ref":"action/divide-instead-of-stall","candidate_revision":"de3ef94988bc98ad69e5fde04468ddd1fcc9b013","desired_result":"Add a split Agent Ask intent so a session that can only finish part of an Action narrows it to the finished slice instead of stalling.","rationale":"This session built and shipped (PR #599) the split settlement mechanism -- criterion 2 -- but ran out of scope to also build the go-side refusal check for criterion 1, which touches evaluateExistingCandidate in src/commands/go.ts and needs its own design pass on what recording an external blocker means. Per the Action's own divide-instead-of-stall philosophy, narrow to the finished slice and queue the remainder right after it.","acceptance":["A split settlement marks the finished slice done and places remainder Actions immediately after it in the queue."],"evidence":[{"criterion":"A split settlement marks the finished slice done and places remainder Actions immediately after it in the queue.","status":"met","note":"Implemented the split Agent Ask intent (src/ask/agentAsk.ts, src/ask/settlement.ts), published it via arcadia agent-ask contract, documented it in AGENTS.md/docs/agents-context.md, and covered it with tests/agent-ask-split.test.ts (10 tests: happy path, queue-ordering, and six refusal paths). Merged as pmark/arcadia#599."}],"actions":[{"id":"go-refuses-stale-session-without-question-or-blocker","desired_result":"arcadia go refuses to end a session with the current Action unchanged unless it records an operator question or external blocker.","acceptance":["arcadia go refuses to end a session with the current Action unchanged unless it records an operator question or external blocker."],"dependencies":[]}]}
