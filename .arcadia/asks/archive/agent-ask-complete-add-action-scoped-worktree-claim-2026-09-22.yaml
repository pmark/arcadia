{"agent_ask": "v1", "request_id": "complete-add-action-scoped-worktree-claim-2026-09-22", "project": "arcadia", "intent": "complete", "target_ref": "plan/arcadia-dispatches-a-different-ready-action-to-each-of-several-concurrent#add-action-scoped-worktree-claim", "candidate_revision": "c0d5160e98c1efb9ff1eb66195c0721925f64fbd", "evidence": [{"criterion": "A new active-claim lookup keyed on (repository_path, project, action_id) coexists with the existing (repository_path, worktree_path) uniqueness on agent_worktree_reservations; neither constraint can be satisfied by breaking the other.", "status": "met"}, {"criterion": "The Action-id conflict lookup filters expires_at > now in the query itself, matching getActiveWorktreeReservation's existing discipline, not only via delete-on-insert cleanup.", "status": "met"}, {"criterion": "Each claim carries a generation (monotonic counter or fresh id per claim, matching how reserveAgentWorktree already replaces rather than reuses a row).", "status": "met"}, {"criterion": "A settlement whose generation does not match the claim's current generation fails loudly and writes nothing; the generation check and the settlement's writes are the same atomic operation (inside transitionActionPointer's existing db.transaction, per the follow-up bound in dispatch-different-ready-action-per-session), never a check followed by a separate write.", "status": "met"}, {"criterion": "A claim is released only by a settlement that completes with apply: true, or by worktree/Session preparation that fails outright before returning its error; both release paths condition their delete atomically on (repository_path, project, action_id, generation) matching, and are no-ops on an already-released claim or a claim whose generation has since moved on.", "status": "met"}, {"criterion": "The 24-hour TTL remains as fallback cleanup only, for an owning process that genuinely died mid-work -- never primary cleanup for a normal completion or a normal preparation failure.", "status": "met"}, {"criterion": "Claiming an already-actively-claimed Action is a hard refusal (extending evaluateExistingCandidate's existing shape), never an advisory flag a caller can act past.", "status": "met"}, {"criterion": "Deterministic tests cover: two claims for the same Action racing (one wins, one refuses); claim release on successful settlement; claim removal on failed preparation; a stale-generation settlement refusing to write; an already-released or superseded-generation release being a no-op; and the worktree-path and Action-id uniqueness constraints being independently enforceable.", "status": "met"}, {"criterion": "pnpm test and the core, Discord and Dashboard builds pass.", "status": "met"}], "desired_result": "Mark add-action-scoped-worktree-claim complete."}
