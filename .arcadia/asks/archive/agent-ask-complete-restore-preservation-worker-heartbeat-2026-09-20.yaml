{"agent_ask":"v1","request_id":"complete-restore-preservation-worker-heartbeat-2026-09-20","project":"arcadia","intent":"complete","target_ref":"restore-preservation-worker-heartbeat","candidate_revision":"6d7593434e746847410d13843a790c7d31ee4d60","desired_result":"Mark restore-preservation-worker-heartbeat complete and resolve the next governed Action.","evidence":[
{"criterion":"After `restart-services.sh restart` reports the worker running, `arcadia go-broker status --json` reports `preservationTransport.ready: true` and a usable Go transport state for the configured workspace.","status":"met","note":"scripts/services.sh restart (run from the main checkout) reported 'Arcadia services are ready' with worker pid 57880; ten seconds later pnpm arcadia go-broker status --json reported preservationTransport.ready true and agentGoTransport.state ready. A first restart attempt failed on a transient 2s dashboard health-probe curl timeout and stopped its services; the immediate retry succeeded. The worker heartbeat publishing and refresh themselves predate this Action (#412); this Action makes worker install verify them."},
{"criterion":"A deterministic regression test proves the worker publishes a preservation heartbeat after startup and that a missing or stale heartbeat fails with a diagnostic that identifies the worker route.","status":"met","note":"tests/preservation-heartbeat-freshness.test.ts: processPreservationRequests publishes the heartbeat (existing test), and the new test asserts transportHeartbeatDiagnostic names the preservation or go route and the heartbeat file for a missing heartbeat and for a 60s-stale one. requestCandidatePreservation now includes that diagnostic in its refusal. Full vitest: 1799 passed; the only non-environmental failures (cli-response-contracts timeout, packageBoundary cold build) pass on rerun; three files fail to load only because this worktree lacks discord.js and next dependencies."}]}
