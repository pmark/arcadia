{"agent_ask":"v1","request_id":"complete-approval-must-apply-or-refuse-2026-09-20-v2","project":"arcadia","intent":"complete","requested_authority":"apply_if_approved","target_ref":"action/approval-must-apply-or-refuse","candidate_revision":"cf9bdf6cbea2d082a1f982c899aa6f0b3788cea9","desired_result":"Record that review approval either writes its authoritative Decision document or refuses before consuming its queue item.","evidence":[{"criterion":"Approving a review item writes the resulting state to the authoritative checked-in document, or refuses; the workspace database and that document never disagree about whether a Decision is answered.","status":"met","note":"tests/review-approval-applies.test.ts verifies status and answer in both the Decision document and review-item database record; 9 focused tests pass."},{"criterion":"An approval whose effect cannot be applied is refused with a reason naming what is missing, and the item remains in the attention queue rather than leaving it.","status":"met","note":"The focused suite verifies missing and malformed Decision document references refuse while the review item remains open."},{"criterion":"A `project_update` Ask whose `target_ref` names a field with no apply path is refused at preview time, rather than opening a clarification Decision that approval cannot act on.","status":"met","note":"src/ask/settlement.ts refuses unsupported project_update target_ref values with its supported targets and remedy; Agent Ask tests pass."},{"criterion":"A regression test reproduces R183: approve a project-field clarification and assert either the field moved and the document was updated, or the approval was refused and the item is still queued.","status":"met","note":"tests/review-approval-applies.test.ts covers the R183 authoritative-write and refusal paths; 9 focused tests pass."},{"criterion":"Preserve the proof Artifact: the regression test plus a before/after of the database and document state; include the exact runnable target and operator QA steps in the pull request.","status":"met","note":"Commit cf9bdf6c (PR #447) includes tests/review-approval-applies.test.ts and records the runnable target and full-suite/lint proof in its PR commit message; rerun with pnpm vitest run tests/review-approval-applies.test.ts."}]}
