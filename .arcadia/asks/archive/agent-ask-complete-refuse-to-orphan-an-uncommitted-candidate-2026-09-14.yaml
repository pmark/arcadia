{
  "agent_ask": "v1",
  "request_id": "complete-refuse-to-orphan-an-uncommitted-candidate-2026-09-14",
  "project": "arcadia",
  "intent": "complete",
  "target_ref": "action/refuse-to-orphan-an-uncommitted-candidate",
  "desired_result": "Mark refuse-to-orphan-an-uncommitted-candidate done: go now detects and refuses to duplicate an uncommitted candidate for the current Action, resumes it per Decision 0051 once proven terminal, and reports clutter accurately.",
  "candidate_revision": "f1e9190b9bbfffa621442a0bb5979dab5fe0b8ec",
  "evidence": [
    {"criterion": "`go` detects a prepared worktree for the current Action that holds uncommitted changes, and reports it with its exact path rather than preparing a second worktree for that Action.", "status": "met", "note": "src/commands/go.ts findUncommittedManualCandidate() scans git worktree list for a branch matching this Action/agent's naming pattern with uncommitted changes and no DB-tracked session; evaluateExistingCandidate() throws a validationError naming its exact worktreePath. Covered by tests/go.test.ts: \"reports the exact path of a manually-prepared candidate holding uncommitted changes instead of preparing a second worktree for the same Action.\""},
    {"criterion": "Per Decision 0051, when the same governed Action still owns the candidate and its prior Session is proven terminal, `go` resumes that candidate (same worktree and branch, repository lease handed over) without operator Git steps. In every other case - different Action, unproven exit, or a conflicting live Session - the reported state names the operator's choices explicitly (preserve the existing candidate, or discard it) and `go` takes neither action implicitly.", "status": "met", "note": "evaluateExistingCandidate() in src/commands/go.ts: a getResumableLeaseHandoff() match for the same actionId with no live tmux session resumes in place (no new worktree/branch, reservation refreshed); a match for a different actionId, a live tmux session, or a dead-but-unreconciled repository lease each refuse with the exact worktreePath and explicit remedy. Covered by tests/session-reconciliation.test.ts describe(\"arcadia go — candidate continuation (Decision 0051)\"): \"resumes the same worktree and branch for the same Action once its prior Session is proven terminal\", \"refuses a new worktree when the repository holds an unresolved resumable candidate for a different Action\", \"refuses a new worktree while a Session is still live\", \"refuses a new worktree over a dead-but-unreconciled Session instead of guessing it is terminal.\""},
    {"criterion": "The `clutter` summary counts existing agent worktrees accurately; a run with two agent worktrees never reports `extraWorktrees: 0`.", "status": "met", "note": "protectedWorktreePaths() in src/commands/go.ts no longer shields every worktree with an unexpired 24h reservation (the prior blanket rule that let two abandoned worktrees each shield the other); it now shields only a worktree backing a genuinely live tmux session plus this run's own freshly prepared worktree. Covered by tests/go.test.ts: \"counts an abandoned agent worktree as clutter instead of shielding it away by its own unexpired reservation\", asserting extraWorktrees === 1 with two agent worktrees present."},
    {"criterion": "Preserve the proof Artifact: fixture tests reproducing an uncommitted prepared worktree, asserting `go` resumes it for the same Action after a proven terminal Session and refuses to duplicate or resume it otherwise; include the exact runnable target and operator QA steps in the pull request.", "status": "met", "note": "Six new fixture tests across tests/go.test.ts and tests/session-reconciliation.test.ts (listed in the two notes above) exercise every branch: manual uncommitted duplicate, resume-after-terminal, different-Action refusal, live-Session refusal, unreconciled-exit refusal, and clutter accuracy. All 1592 repository tests pass (pnpm exec vitest run). The pull request states the exact runnable target and operator QA steps."}
  ]
}
