{
  "agent_ask": "v1",
  "request_id": "priority-is-queue-derived-2026-09-16-v2",
  "project": "arcadia",
  "intent": "decision",
  "requested_authority": "propose",
  "desired_result": "Should plan and Action priority live only in the queue?",
  "rationale": "A priority Decision carries no reviving trigger, so it binds today to a past ordering the moment it is written; the live queue already re-derives priority at every dispatch, so such a Decision is structurally stale and spends operator attention without changing what happens next. Arcadia's own deferral rule (\"If not now, then when?\") requires a named trigger for anything postponed, and a priority choice has none. Encoding priority in the queue keeps it continuously correct instead of periodically re-litigated. This is the guardrail companion to Decision 0053: 0053 says auto-apply the obvious and escalate judgment; this says priority is never judgment, it is queue state.",
  "options": [
    {
      "label": "Priority lives in the queue",
      "consequence": "Priority-only questions are closed as superseded as soon as they are written; priority requests route to advance queue moves (top/before/after); no priority-only Decision opens again. Any genuine dependency, acceptance, or capability question still escalates normally under 0053.",
      "recommended": true
    },
    {
      "label": "Keep priority Decisions",
      "consequence": "Priority questions keep accumulating in Waiting on you and must be re-answered whenever they go stale, even though the queue already determines dispatch order."
    }
  ]
}
