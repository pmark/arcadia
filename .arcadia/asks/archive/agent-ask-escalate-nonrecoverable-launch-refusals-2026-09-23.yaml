{
  "agent_ask": "v1",
  "request_id": "escalate-nonrecoverable-launch-refusals-2026-09-23",
  "project": "arcadia",
  "intent": "action",
  "desired_result": "The worker stops treating a non-self-resolving launch refusal the same as a transient wait-state, and surfaces it to the operator instead of retrying silently forever.",
  "rationale": "Found live during the 2026-09-23 concurrency strategy session: the pointer Action register-agent-workspace-trust retried every ~25-30s for over an hour with zero progress and zero operator-visible signal, because its packet is planning_required -- a state no future tick resolves on its own. src/production/tick.ts:388-394 buckets that refusal with capacity/Off/stale-preview/lease conflicts, which really do self-resolve. Filed as Issue #576 with full evidence. This is the single highest-leverage fix available on the existing single-lane critical path: it does not add concurrency, it makes the one lane's stalls legible instead of silent.",
  "actions": [
    {
      "id": "escalate-nonrecoverable-launch-refusals",
      "desired_result": "Distinguish a non-self-resolving launch refusal (starting with planning_required) from a transient wait-state conflict, and surface it to the operator instead of retrying silently forever.",
      "acceptance": [
        "production/tick.ts's conflict-refusal handling classifies planning_required, and any other refusal whose remedy requires an operator or agent action rather than the passage of time, separately from capacity/Off/stale-preview/lease conflicts.",
        "A non-self-resolving refusal is surfaced once per a bounded window (not on every tick) through a durable, operator-visible signal -- the existing /runs terminal-approvals surface or an equivalent named mechanism -- rather than appearing only as routine worker.out.log noise.",
        "A deterministic test reproduces both cases: a transient conflict (e.g. capacity) keeps retrying silently exactly as today; a planning_required refusal is surfaced once and does not repeat the same signal on every subsequent tick while it remains unresolved.",
        "pnpm test and the core, Discord and Dashboard builds pass."
      ],
      "dependencies": [],
      "references": ["src/production/tick.ts", "src/sessions/launch.ts", "https://github.com/pmark/arcadia/issues/576"]
    }
  ]
}
