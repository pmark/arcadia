{"agent_ask": "v1", "request_id": "complete-resolve-cross-plan-dependency-ids-2026-09-26", "project": "arcadia", "intent": "complete", "target_ref": "action/resolve-cross-plan-dependency-ids", "candidate_revision": "025afac898c06bba9b10eb34e54540d3b9f293ad", "evidence": [{"criterion": "canonicalOrder (src/scheduling/order.ts) and collectUnmetDependencies (src/docs/dispatch.ts) resolve a depends_on id first within the same Plan, then across the same Project's Plans as plan/<slug>#<action-id>; an id that resolves to a done Action on the base branch is satisfied in both.", "status": "met"}, {"criterion": "An id that does not resolve to any known Action in the Project is never treated as satisfied (dependency_unresolved): collectUnmetDependencies reports it as an unmet blocker, so deriveStatus (src/scheduling/schedule.ts) and dispatch never mark the Action ready, and canonicalOrder holds the candidate behind every candidate that can proceed.", "status": "met"}, {"criterion": "The plan parser (src/docs/parse.ts) accepts plan/<slug>#<action-id> dependency ids without reporting them as dangling; a bare id outside the same Plan is still reported as dangling.", "status": "met"}, {"criterion": "A dependency on a deferred Action is reported with its actual status (deferred), not dependency_unresolved.", "status": "met"}, {"criterion": "Deterministic tests cover: a same-Plan dependency, a cross-Plan dependency, a dependency that lands after being unresolved on an earlier tick, an id that never resolves, a dependency chain that would cycle across Plans staying safe and never looping, and a dependency on a deferred Action; pnpm test and the core, Discord and Dashboard builds pass.", "status": "met"}], "desired_result": "Mark resolve-cross-plan-dependency-ids complete."}
