{"agent_ask":"v1","request_id":"promote-fix-preservation-worker-heartbeat-2026-09-19","project":"arcadia","intent":"action","requested_authority":"apply_if_approved","desired_result":"Promote GitHub issue #411 as a stop-the-line repair so protected preservation reliably becomes available after managed worker startup.","rationale":"The running worker does not publish a fresh preservation or Go-route heartbeat, blocking every prepared candidate from protected preservation and therefore from completion handoff.","actions":[{"id":"restore-preservation-worker-heartbeat","desired_result":"Make the managed Arcadia worker publish and maintain a fresh preservation and Go-route heartbeat after restart, with deterministic coverage for startup and stale-heartbeat refusal paths.","acceptance":["After `restart-services.sh restart` reports the worker running, `arcadia go-broker status --json` reports `preservationTransport.ready: true` and a usable Go transport state for the configured workspace.","A deterministic regression test proves the worker publishes a preservation heartbeat after startup and that a missing or stale heartbeat fails with a diagnostic that identifies the worker route."],"dependencies":[],"references":["https://github.com/pmark/arcadia/issues/411"]}]}
