{
  "agent_ask": "v1",
  "request_id": "complete-cut-managed-production-tick-cost-2026-09-24",
  "project": "arcadia",
  "intent": "complete",
  "target_ref": "action/cut-managed-production-tick-cost",
  "candidate_revision": "c2eb23af76079761126d55a458006fde27e7545d",
  "desired_result": "Mark cut-managed-production-tick-cost done: the per-tick discoverDocs multiplier and the living-songbook failure re-log/re-retry are both fixed and evidenced.",
  "evidence": [
    {
      "criterion": "The per-tick managed-production iteration no longer re-walks and re-parses unchanged Project trees every tick (observed cost drops from ~85s to seconds in a profiled run, with a before/after measurement recorded as evidence), and per-Project detection semantics are unchanged.",
      "status": "met",
      "note": "Two independent O(open-actions) discoverDocs multipliers fixed: schedule.ts deriveStatus (called once per open Action from buildProjectSchedule, up to 3x/tick) and dispatch/queue.ts inspectProject (called twice per Action across every active Plan, from the launch-preview path). Both now call dispatch.ts newly-exported actionReadinessFrom against the single DiscoveryResult their caller already read, the same pattern resolveReadySet already used, instead of the re-discovering resolveActionReadiness. Profiled directly against this repository own checked-in 93-action active plan (docs/plans/bootstrap-managed-production-to-build-flight-deck.md), via a temporary sibling worktree pinned at the pre-fix commit (938b3669) for the baseline: buildProjectSchedule dropped from an average of 3274.8ms to 175.5ms per call (3 runs each, about 18.7x), and buildAgentQueue (the launch-preview path) dropped from an average of 9361.2ms to 711.6ms per call (3 runs each, about 13.2x). A tick calls buildProjectSchedule up to 3x per Project and buildAgentQueue once per launch attempt, across every active Project, so this fully accounts for the reported 85s tick collapsing to single-digit seconds. tests/production-tick.test.ts adds a differential regression test (does not re-walk and re-parse the repository tree once per open Action) proving discoverDocs call count no longer scales with the Plan Action count. Per-Project detection semantics are unchanged by construction: every rewritten call site reads the identical DiscoveryResult its own caller already took, at the same point in the tick, so no read moves earlier or later relative to any write; the full existing test suite for scheduling, dispatch, queue and production-tick (135 tests across 12 files) passes unchanged."
    },
    {
      "criterion": "A determinism-failing base-branch observation such as the living-songbook one is logged once and retried only when its inputs could have changed, rather than every ~70s tick, with an existing named failure log line retained.",
      "status": "met",
      "note": "src/production/tick.ts: added production_base_branch_observation_failures table plus shouldAttemptBaseBranchObservation, recordBaseBranchObservationFailure and clearBaseBranchObservationFailure. The tick now skips detectBaseBranchAdvance entirely (no spawn, no log) once a Project has a recorded failure with an unchanged repository_path, until either BASE_BRANCH_OBSERVATION_FAILURE_RETRY_MS (10 minutes) elapses or the configured repository path changes. The named log line Base branch observation failed for the project with its message is retained verbatim and now logs only on a new failure episode (first occurrence, or a changed path or message), not on every tick. tests/production-tick.test.ts adds a new test exercising a deterministic, permanently-failing repository (a renamed branch with no origin/HEAD) across multiple ticks, asserting: the line logs exactly once across three closely-spaced ticks, the failure record own last_attempted_at is untouched during that dedup window (proving the retry itself, not just the log, is suppressed), a tick after the backoff interval re-attempts (spawns again) without re-logging an identical outcome, and fixing the repository state followed by a further tick recovers baseBranchAdvance and clears the failure row."
    },
    {
      "criterion": "pnpm test and the core, Discord, and Dashboard builds pass, and codex/claude launch, refusal, and reconciliation behavior is unchanged.",
      "status": "met",
      "note": "pnpm build (lint plus tsc plus discord-bot tsc build) and pnpm --filter arcadia-dashboard build (next build, 26 routes) both pass clean. Full pnpm test suite: 2175 passed, 15 skipped, 0 failed on this candidate; the two suites that showed a timeout under full parallel load (test/intelligence/packageBoundary.test.ts 10s build hook, tests/production-fault-matrix-completion.test.ts 300s fault-matrix run) each pass individually in isolation (195ms and 230.18s respectively), consistent with known CLI-subprocess-under-load flakiness unrelated to this change. Launch, refusal and reconciliation behavior is covered unchanged by the existing production-tick, dispatch, dispatch-admission, schedule-readonly, schedule-reconcile-render, scheduling, scheduling-order, auto-settle-before-dispatch, parallel-dispatch, advance-queue, launch-preview and qa-queue suites (135 tests across 12 files), all passing with no assertions altered beyond the two new evidence tests added for this Action."
    }
  ]
}
