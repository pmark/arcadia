{"agent_ask": "v1", "request_id": "harden-live-blocker-acceptance-2026-09-24", "project": "arcadia", "intent": "plan", "target_ref": "plan/bootstrap-managed-production-to-build-flight-deck", "requested_authority": "propose", "desired_result": "Tighten three newly filed production-blocker Actions so their acceptance cannot be met by an advisory or forgeable fix.", "rationale": "CodeRabbit review of PR #618: a brief-only retry limit, an environment- or cwd-based Session check, and a merely longer heartbeat threshold would each satisfy the criteria as written while leaving the live failure reachable.", "actions": [{"target_ref": "action/name-failing-preservation-check-and-bound-retries", "desired_result": "Make a preservation refusal name the check that failed, and stop a Session from retrying an identical refusal forever.", "acceptance": ["The 'Declared preservation validation failed or was skipped.' refusal returned by the preserve broker carries non-empty details naming each failing or skipped check, its command, and its exit status or skip reason.", "The preserve broker or Session controller, not only the Session brief, enforces a bounded number of identical preservation refusals per Session; when the limit is reached it records the refusal and the Session is reconciled as an incomplete exit.", "Deterministic tests cover a failing check (details name it), a skipped check (details name the skip reason), and a Session that reaches the identical-refusal limit and is recorded incomplete.", "pnpm test and the core, Discord and Dashboard builds pass, and the PR refs Issue #611."]}, {"target_ref": "action/withhold-worker-lifecycle-from-sessions", "desired_result": "Stop a dispatched coding-agent Session from stopping, starting, or restarting the shared host worker.", "acceptance": ["arcadia worker stop, start, restart and install refuse, with a named reason, when the caller descends from a managed coding-agent Session, determined by a non-forgeable host-side check (such as the Session's recorded process tree or tmux server) rather than caller environment variables or working directory.", "The operator's own terminal and the launchd agent can still run every worker command unchanged.", "A deterministic test proves the refusal from a Session process even after it clears its environment and changes directory to the host workspace, and proves the unchanged operator and launchd paths.", "pnpm test and the core, Discord and Dashboard builds pass, and the PR closes Issue #611 together with name-failing-preservation-check-and-bound-retries or refs it if that Action has not merged."]}, {"target_ref": "action/stop-killing-busy-workers", "desired_result": "Recover only a genuinely hung worker, never one that is merely busy inside a long synchronous step.", "acceptance": ["Hung-worker recovery in arcadia worker start and stop is driven by a liveness signal that no synchronous tick step can starve (for example a beat from a separate thread or process), not by lengthening the heartbeat threshold alone.", "A deterministic test holds the worker inside a synchronous step for longer than 26s and proves start neither signals nor replaces it, while a worker that stops progressing entirely is still recovered.", "The 170s and 292s stalls recorded in Issue #617 are investigated and their cause recorded in the PR, or recorded as not reproducible with the evidence gathered.", "pnpm test and the core, Discord and Dashboard builds pass, and the PR closes Issue #617."]}]}
