{
  "agent_ask": "v1",
  "request_id": "action-plan-scoped-agent-ask-complete-2026-09-22",
  "project": "arcadia",
  "intent": "action",
  "desired_result": "arcadia agent-ask settle --intent complete accepts target_ref: plan/<plan-slug>#<action-id> to complete an Action in any of the Project's plans, not only the active one, without writing PROJECT.md or disturbing the active plan's own current_action/queue when the completed Action belongs to a different, non-active plan. Plain action/<id> keeps meaning the active plan, unchanged.",
  "rationale": "Unblocks starting the parallel-session-dispatch draft plan (arcadia-dispatches-a-different-ready-action-to-each-of-several-concurrent) without activating it and stranding the active plan's own in-flight work, including serialize-current-action-writes. Scoped and prototyped by a dedicated Opus investigation on 2026-09-22, confirmed genuinely small (~40 lines in src/ask/settlement.ts's complete case plus one helper), confirmed via source that resolveActionReadiness already searches every plan in the Project by Action id (src/docs/dispatch.ts), and confirmed that activating the draft plan would symmetrically strand the active plan's own Actions (settlement.ts's activation path already resets the outgoing plan's current_action to null). Deliberately excludes any change to automatic dispatch, the queue, or arcadia go's worktree selection -- those remain the separate draft plan's job.",
  "actions": [
    {
      "id": "plan-scoped-agent-ask-complete",
      "desired_result": "arcadia agent-ask settle --intent complete accepts target_ref: plan/<plan-slug>#<action-id>, resolving and completing that Action in its own (possibly non-active) plan, writing PROJECT.md and the active plan's current_action only when the completed Action is actually in the active plan.",
      "acceptance": [
        "target_ref of the form plan/<plan-slug>#<action-id> resolves and completes that Action in the named plan, regardless of whether that plan is the Project's active_plan; plain action/<id> is unchanged and continues to mean the active plan.",
        "Completing an Action in a non-active plan does not write PROJECT.md and does not change the active plan's current_action or queue; the settlement records that the pointer and active plan were left untouched.",
        "A non-unique Action id across the Project's plans is refused with a clear error, matching the existing guard used by plan activation.",
        "Every other complete-intent validation is unchanged for both forms: evidence must cover every declared acceptance criterion verbatim and in order, all evidence must be met, candidate_revision must match HEAD, unresolved required review Decisions refuse completion, and the existing clean-tree/preview-fingerprint/replay-receipt/Mission-Log behavior is preserved.",
        "arcadia agent-ask contract's complete example and AGENTS.md's complete-intent description both document the plan/<slug>#<action-id> form, not only action/<id>.",
        "Regression tests cover: completing an Action in a non-active plan while a different plan stays active throughout, asserting the active plan's document and PROJECT.md are byte-for-byte unchanged; and refusing an ambiguous or unresolvable plan-scoped target_ref.",
        "pnpm test and the core, Discord and Dashboard builds pass."
      ],
      "dependencies": []
    }
  ]
}
