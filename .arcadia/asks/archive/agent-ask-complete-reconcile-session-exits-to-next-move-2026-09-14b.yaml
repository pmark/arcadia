{
  "agent_ask": "v1",
  "request_id": "complete-reconcile-session-exits-to-next-move-2026-09-14b",
  "project": "arcadia",
  "intent": "complete",
  "target_ref": "action/reconcile-session-exits-to-next-move",
  "desired_result": "Mark reconcile-session-exits-to-next-move done: a dead Session's exit is reconciled into a durable receipt and the resulting canonical next move, with the quality-gate negative cases proven.",
  "candidate_revision": "5ffd3864e0d7d31cb191a4c1b4b202be349ca875",
  "evidence": [
    {"criterion": "Persist a thin exit observation/receipt through the existing operational model and link available Run, Artifact and Decision proof.", "status": "met"},
    {"criterion": "Distinguish successful exit, failed execution, missing evidence, needs input, incomplete with a resumable candidate, and accepted Action completion; zero exit never marks done by itself.", "status": "met"},
    {"criterion": "Release repository leases only on proven terminal state and preserve recoverable reconciliation errors. Per Decision 0051, an incomplete exit whose candidate is resumable hands the existing repository lease to the next Session for the same Action rather than releasing it to a competing preparation; no new lease type is introduced.", "status": "met"},
    {"criterion": "Display the resulting canonical next Action or exact judgment/blocker with a usable link; automatic next admission only under the current Active production policy; no hand-edited governance state.", "status": "met"},
    {"criterion": "Retry/reconcile/reload is idempotent and does not duplicate completion, Decisions or execution.", "status": "met"},
    {"criterion": "Preserve the proof Artifact: Exit-to-evidence-to-next-move lifecycle integration tests; include exact runnable target and operator QA steps in the PR, or state why no runnable surface exists.", "status": "met"},
    {"criterion": "Require criterion-level evidence bound to the exact Candidate revision and a separate review pass for nontrivial code and safety boundaries; unresolved blocking findings, missing/skipped checks and stale evidence prevent acceptance.", "status": "met"},
    {"criterion": "Prove the quality gate rejects deliberately failed tests, absent artifacts and false agent completion claims as specified in contract 20.", "status": "met"}
  ],
  "rationale": "The bulk of this Action (src/sessions/reconciliation.ts, tests/session-reconciliation.test.ts) was already merged in commit 4851953 (PR #251) from a prior session, which satisfied criteria a/b/c/d/e and most of f. This session found and closed the remaining gaps via an independent code review (2 finder agents + verification, reported through ReportFindings): (1) probeExitEvidence counted the most recent execution_runs row as valid completion evidence regardless of its status, so a deliberately failed or requires_review Run could be misclassified as successful_exit -- fixed by gating classification on run status while still linking the actual Run/artifact for audit purposes even when it failed; (2) reused the existing isRequiresReviewValue helper instead of a new hardcoded string check; (3) added a test proving a failed Run is rejected as evidence (closing criterion h's 'deliberately failed test' case; the 'absent artifact' and 'false completion claim' cases were already covered by the pre-existing missing_evidence and accepted_completion tests); (4) documented `arcadia session reconcile` with operator QA steps in docs/COMMANDS.md, closing the remaining gap in criterion f. A third review finding -- prepareSession's lease-handoff check-then-write in src/sessions/index.ts isn't wrapped in a transaction, a plausible concurrency race for two simultaneous session launches on the same repository -- was judged out of scope for this Action (it belongs to session admission/launch, not exit reconciliation) and spun off as a separate follow-up task rather than expanding this Action's scope. The full test suite (1570 passed, 13 skipped) and typecheck pass clean at the candidate revision. Contract 20's broader fault-injection matrix (100-repeat races, live rehearsals, soak tests) is explicitly scoped to later, larger Actions per that document's own text and is not claimed here -- only the three specific negative cases this criterion names for the reconciliation layer are proven."
}
