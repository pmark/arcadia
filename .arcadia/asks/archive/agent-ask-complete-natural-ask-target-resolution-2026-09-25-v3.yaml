{"agent_ask": "v1", "request_id": "complete-natural-ask-target-resolution-2026-09-25-v3", "project": "arcadia", "intent": "complete", "target_ref": "plan/agent-ask-execution-queue#make-a-natural-language-agent-ask-propose-the-concrete-canonical-effect-when-the", "desired_result": "Merged PR #637: a natural Agent Ask now resolves its free text against existing checked-in Plan/Action/Decision identifiers and proposes the concrete effect instead of always falling back to a generic interpretation Decision.", "rationale": "resolveNaturalAgentAskTarget (src/ask/agentAsk.ts) matches identifiers deterministically against git HEAD, with Action taking priority over a co-mentioned Plan and genuine conflicts/ambiguity falling back to the existing interpretation path. Settlement opens a focused Decision naming the resolved target. CodeRabbit approved after two fix rounds; all required checks green; squash-merged as c441d915.", "candidate_revision": "c441d9152903e08cea36b28103cb00ec3ad025c4", "evidence": [{"criterion": "When natural text names an existing plan slug, Action id, or Decision id that resolves in the destination Project, the preview proposes the specific create or amend effect against that target rather than a bare interpretation effect.", "status": "met", "note": "buildAgentAskEffects sets operation update and targetKind/targetRef to the resolved plan/action/decision when resolveNaturalAgentAskTarget succeeds; covered by tests/agent-ask.test.ts resolved-plan/action/decision cases."}, {"criterion": "Resolution is deterministic and makes zero model calls; only exact identifiers already present in checked-in documents are matched, and no priority, date, dependency, approval, or Project ownership is inferred.", "status": "met", "note": "resolveNaturalAgentAskTarget is a pure regex-based function with no model call; buildTargetContext in src/ask/preview.ts reads only git HEAD (git ls-tree/show), never the working tree, so untracked or uncommitted edits never resolve."}, {"criterion": "When no identifier resolves, or more than one resolves ambiguously, the existing interpretation Decision path is preserved unchanged, and the receipt names the candidates it considered and rejected.", "status": "met", "note": "Ambiguous/conflicting matches return resolved: null and fall through to the unchanged interpretation effect; considered-but-not-chosen candidates are surfaced on proposal.refused. Covered by the unresolvable and ambiguous tests in tests/agent-ask.test.ts."}, {"criterion": "A resolved proposal still requires operator acceptance and still opens a focused Decision whenever it would materially change Project ownership, acceptance criteria, authority, or queue position.", "status": "met", "note": "settleAgentAsk's auto case always opens a Decision (never applies anything automatically); when resolved it names the concrete target and preserves the target's own Plan. Covered by the end-to-end settlement tests in tests/agent-ask-settlement.test.ts."}, {"criterion": "Focused tests cover a resolved plan reference, a resolved Action reference, a resolved Decision reference, an unresolvable reference, an ambiguous reference, and byte-stable replay of each.", "status": "met", "note": "tests/agent-ask.test.ts adds all six cases plus uncommitted-document and boundary-safety cases; tests/agent-ask-settlement.test.ts adds end-to-end settlement coverage including cross-Plan Decision resolution."}], "requested_authority": "apply_if_approved"}
