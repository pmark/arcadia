{"agent_ask":"v1","request_id":"complete-detect-hung-managed-production-sessions-2026-09-22","project":"arcadia","intent":"complete","desired_result":"Mark detect-hung-managed-production-sessions done: the managed-production worker tick now flags a live-but-stalled Session on its own tmux/Run activity signal, preserving its lease, without touching dead-Session reconciliation.","target_ref":"action/detect-hung-managed-production-sessions","candidate_revision":"8154a887340975708b55c079838c4c85844ce5ea","evidence":[{"criterion":"Define an observable, deterministic signal for 'stalled' (e.g. no new tmux pane output, no new Run/receipt activity) and a bounded deadline before a live-but-stalled Session is flagged.","status":"met","note":"src/production/stallDetection.ts observeSessionActivity hashes tmux capturePane output plus the latest execution_runs status/updated_at into a signature; PRODUCTION_CONTROL_DEADLINES.stalledSessionDeadlineMs (20 min, set above providerCallDeadlineMs) bounds how long an unchanged signature is tolerated before flagging."},{"criterion":"A flagged stalled Session is surfaced as an explicit uncertain/needs-attention state, never silently reconciled as successful or silently relaunched.","status":"met","note":"stall_flagged_at is a new agent_sessions column, never fed into reconcileSessionExit; sessionView() reports observedStatus 'stalled' while live and flagged, and the dashboard colors it in the failed/blocked (attention) bucket. A managed_production.session_stalled event is recorded once per flag."},{"criterion":"The existing repository lease and admission are preserved (not released) while a Session is only suspected stalled, pending operator or bounded automatic repair.","status":"met","note":"observeSessionActivity never updates agent_sessions.status; getRepositoryLease only excludes rows outside status IN (prepared, running), so a flagged Session keeps its lease and attemptProjectLaunch continues to skip admission with 'Repository lease held by Session ...'. Covered by the new production-tick test asserting launch.outcome stays skipped and status stays running after flagging."},{"criterion":"False positives are bounded: a Session doing real long-running work is not flagged merely for being slow.","status":"met","note":"The signature changes whenever pane output or Run state changes, resetting the activity clock; covered by the new 'never flags a live Session doing real long-running work' test spanning 2x the stall deadline with continuously changing pane output and asserting stall_flagged_at stays null and no event is recorded."}]}
