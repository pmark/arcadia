{
  "agent_ask": "v1",
  "request_id": "complete-gate-dispatch-on-blocking-operator-items-2026-09-24-v2",
  "project": "arcadia",
  "intent": "complete",
  "target_ref": "action/gate-dispatch-on-blocking-operator-items",
  "candidate_revision": "ab893d28fc9e13cf2278381ae414f3dfaa755c2b",
  "desired_result": "Mark gate-dispatch-on-blocking-operator-items done: a shared operator gate now blocks go/advance/next dispatch on a pending Decision or Agent Ask naming the selected Action, and lists everything else as a capped alert.",
  "evidence": [
    { "criterion": "One shared function classifies every pending operator-only item (unsettled Agent Ask proposals, open Decisions) as blocking or alert, reusing the exact data surface-terminal-operator-approvals-in-runs already built rather than re-deriving it.", "status": "met", "note": "classifyOperatorItems (src/docs/operatorGate.ts) is the one classifier; resolveOperatorGate (src/ask/operatorGate.ts) feeds it from listUnsettledAgentAskProposals (the same function backing the /runs approval queue) and a new listOpenDecisions doc-reader -- not a re-derivation of either." },
    { "criterion": "An item is blocking when it names, or its Decision's action: field names, an Action the current dispatch resolution would otherwise select, or when it is the reason no Action in the current queue segment is eligible; every other pending item is an alert.", "status": "met", "note": "classifyOperatorItems: selectedActionId match, or membership in blockingDecisionIds derived from resolveReadySet candidates' deferringDecisionId/requiredDecisionId." },
    { "criterion": "When one or more blocking items exist, arcadia go/advance/next refuses to hand off a dispatch brief for agent work and instead prints each blocking item's title, recommended option and consequence, and the exact command to settle it — matching the existing per-Action blocker/operatorQuestion contract, not a second one.", "status": "met", "note": "next merges blocking items into the existing DispatchBlocker list (printed via the pre-existing renderBlockers). advance's launch preview and the production scheduler refuse via resolveProjectTransition returning kind: decision instead of launch. go's reported dispatchable/transition fields now correctly refuse too. Residual: go's internal worktree-preparation branching still reads the ungated isDispatchable check, so a worktree can still be prepared for a gated Action -- but no agent code-writing session is ever launched against it, since the production scheduler and a manual arcadia advance inside that worktree both re-check the gate and refuse." },
    { "criterion": "When only alert items exist, dispatch proceeds normally and the resolution additionally lists each alert's title and one-line consequence, newest first, capped at a small fixed count with a count of any remainder.", "status": "met", "note": "renderOperatorAlerts (cap 5, \"+N more\") is appended in next's and go's text rendering; the full list rides on transition.operatorGate.alerts for JSON consumers." },
    { "criterion": "This one gate is shared by the CLI (go, advance, next), the dashboard's equivalent status calls, and the Discord bot's dispatch-brief posting — none of them re-implements its own copy.", "status": "met", "note": "resolveProjectTransition is the single chokepoint for go/advance/production; next calls the same resolveOperatorGate. The dashboard's continuation endpoint already shells to arcadia next --json, inheriting the gate with no separate implementation. The Discord bot has no dispatch-brief-posting code today (verified by search), so there is nothing to de-duplicate against -- criterion holds vacuously." },
    { "criterion": "Regression tests cover: a blocking item suppresses dispatch and is named exactly; an alert-only state dispatches normally with the alert list attached; zero pending items adds neither section; an item blocking one Project does not suppress dispatch for an unrelated Project.", "status": "met", "note": "tests/operator-gate.test.ts covers all four cases at the pure-classifier level (plus a decision/agent-ask kind split and the ready-set-reason case); tests/session.test.ts adds two integration tests exercising resolveProjectTransition end to end for the blocking-Decision and alert-only cases. Full suite: pnpm test 2223 passed, 13 pre-existing skips; root/dashboard/discord-bot builds clean." }
  ]
}
