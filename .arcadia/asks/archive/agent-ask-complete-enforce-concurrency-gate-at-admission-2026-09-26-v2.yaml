{"agent_ask":"v1","request_id":"complete-enforce-concurrency-gate-at-admission-2026-09-26-v2","project":"arcadia","intent":"complete","target_ref":"action/enforce-concurrency-gate-at-admission","candidate_revision":"9094f1c616ea1fece16f8eb4fd4261f8872c0e5c","evidence":[{"criterion":"issueAdmission (src/production/policy.ts) caps effective concurrency at 1 on every admission, whatever maxConcurrentSessions the stored scope carries, unless plan/bootstrap-managed-production-to-build-flight-deck#prove-two-action-unattended-production and plan/bootstrap-managed-production-to-build-flight-deck#prove-concurrent-ready-set-admission are both done on the base branch; a refused admission names both ids.","status":"met"},{"criterion":"The check runs on every admission and names both Actions by Plan-qualified id, so reopening either Action restores the cap without deactivating the policy, and a later change of active Plan neither lifts nor permanently locks the gate.","status":"met"},{"criterion":"production preview and activate with --concurrency greater than 1 report the effective cap and its reason while the gate is closed, instead of silently recording a limit that will not be honoured.","status":"met"},{"criterion":"The only way to exceed the cap before both proofs are done is an explicit, expiring rehearsal exception on the operator-granted policy scope that names prove-concurrent-ready-set-admission; it lapses at its expiry or on deactivation, and nothing else lifts the cap.","status":"met"},{"criterion":"Deterministic tests cover: the cap holding for a stored scope above 1 while the gate is closed, including a scope written directly without passing through activation; the cap lifting once both Actions are done; the cap returning when one is reopened; and the rehearsal exception being honoured only before its expiry; pnpm test and the core, Discord and Dashboard builds pass.","status":"met"}],"desired_result":"Mark enforce-concurrency-gate-at-admission complete."}
