{"agent_ask": "v1", "request_id": "restore-blocker-references-2026-09-24", "project": "arcadia", "intent": "plan", "target_ref": "plan/bootstrap-managed-production-to-build-flight-deck", "requested_authority": "propose", "desired_result": "Restore the references that the harden-live-blocker-acceptance amendment cleared on two production-blocker Actions.", "rationale": "Amendment references are replacement lists; harden-live-blocker-acceptance-2026-09-24 omitted them and so cleared the Issue and source-file references filed by file-live-production-blockers-2026-09-24. Titles and acceptance are restated verbatim and unchanged.", "actions": [{"target_ref": "action/name-failing-preservation-check-and-bound-retries", "desired_result": "Make a preservation refusal name the check that failed, and stop a Session from retrying an identical refusal forever.", "acceptance": ["The 'Declared preservation validation failed or was skipped.' refusal returned by the preserve broker carries non-empty details naming each failing or skipped check, its command, and its exit status or skip reason.", "The preserve broker or Session controller, not only the Session brief, enforces a bounded number of identical preservation refusals per Session; when the limit is reached it records the refusal and the Session is reconciled as an incomplete exit.", "Deterministic tests cover a failing check (details name it), a skipped check (details name the skip reason), and a Session that reaches the identical-refusal limit and is recorded incomplete.", "pnpm test and the core, Discord and Dashboard builds pass, and the PR refs Issue #611."], "references": ["https://github.com/pmark/arcadia/issues/611", "src/sessions/preservationValidation.ts"]}, {"target_ref": "action/withhold-worker-lifecycle-from-sessions", "desired_result": "Stop a dispatched coding-agent Session from stopping, starting, or restarting the shared host worker.", "acceptance": ["arcadia worker stop, start, restart and install refuse, with a named reason, when the caller descends from a managed coding-agent Session, determined by a non-forgeable host-side check (such as the Session's recorded process tree or tmux server) rather than caller environment variables or working directory.", "The operator's own terminal and the launchd agent can still run every worker command unchanged.", "A deterministic test proves the refusal from a Session process even after it clears its environment and changes directory to the host workspace, and proves the unchanged operator and launchd paths.", "pnpm test and the core, Discord and Dashboard builds pass, and the PR closes Issue #611 together with name-failing-preservation-check-and-bound-retries or refs it if that Action has not merged."], "references": ["https://github.com/pmark/arcadia/issues/611", "src/commands/worker.ts"]}]}
