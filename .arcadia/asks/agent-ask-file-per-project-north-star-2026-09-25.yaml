{"agent_ask": "v1", "request_id": "file-per-project-north-star-2026-09-25", "project": "arcadia", "intent": "action", "requested_authority": "propose", "desired_result": "File support for a NORTH_STAR.md per Project, alongside the portfolio-wide workspace NORTH_STAR, held until automated production is proven.", "rationale": "Operator instruction 2026-09-25: each Project should have its own NORTH_STAR in addition to the workspace-wide one. Today src/northStar/document.ts loads exactly one NORTH_STAR.md from the workspace root and it names a single target Project, so a per-Project file is inert. Per the same instruction, this is off the automated-production critical path and depends on prove-two-action-unattended-production.", "actions": [{"id": "support-per-project-north-star", "desired_result": "Read a NORTH_STAR.md from each Project repository root as that Project's target, alongside the workspace NORTH_STAR as the portfolio-wide target.", "acceptance": ["The north-star loader reads an optional NORTH_STAR.md at each Project repository root with the same schema as the workspace file, taking project from its own Project when the field is omitted, and never replaces or reinterprets the workspace NORTH_STAR.md, which remains the portfolio-wide target.", "arcadia now and the dashboard /now and /path screens can show one Project's target and gates (a --project flag or equivalent selector), while the default view stays the portfolio-wide target.", "A Project NORTH_STAR.md that fails to parse is reported as a named validation issue for that Project only, and does not break the portfolio view or other Projects.", "Deterministic tests cover a Project file, a missing Project file, a malformed Project file, and an unchanged workspace-only setup; pnpm test and the core, Discord and Dashboard builds pass.", "START_HERE.md documents where each NORTH_STAR lives and which view reads it."], "dependencies": ["prove-two-action-unattended-production"], "references": ["src/northStar/document.ts", "src/northStar/compute.ts", "src/northStar/path.ts", "apps/dashboard/app/now/page.tsx", "apps/dashboard/app/path/page.tsx", "NORTH_STAR.md"]}]}
