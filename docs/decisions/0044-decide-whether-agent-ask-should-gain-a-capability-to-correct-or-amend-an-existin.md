---
arcadia: v1
type: decision
id: "0044"
slug: decide-whether-agent-ask-should-gain-a-capability-to-correct-or-amend-an-existin
project: arcadia
status: open
question: Decide whether Agent Ask should gain a capability to correct or amend an existing managed document, and whether settle's full-corpus revalidation on every apply should change. Right now neither exists, and together they mean that once any single managed document anywhere in a governed repo drifts out of schema, every future Agent Ask settlement in that repo is permanently blocked with no path through Arcadia to fix it.
gap_type: missing-decision
recommendation: "DISCOVERED WHILE using Agent Ask against Private Practice Now for the first time this repo has ever exercised it end to end (see the already-settled Decision from request_id agent-action-closeout-has-no-arcadia-path-2026-09-03, project private-practice-now, which this Ask is a direct sequel to). That Decision covered the missing path for closing out a delivered Action. Settling it surfaced a second, more general problem in Arcadia itself, reported here because it belongs to Arcadia's own capability set, not to any one adopting project.\nWHAT HAPPENED. `arcadia agent-ask settle --disposition accepted --apply` failed on its very first real use in Private Practice Now, not because of anything in the settled content, but with `VALIDATION_ERROR: \"Accepted Agent Ask managed documents failed operational sync.\"` and 49 pre-existing errors across the whole repository: 45 MISSION_LOG.md entries missing the literal `**Did:**` / `**Result:**` bullet format (they use a narrative-bullet style the log actually converged on for its last 8 days of entries instead), and 4 docs (`docs/pilot-client-site-inner-page-ux-review.md`, `docs/pilot-client-site-redesign-audit.md`, `docs/pilot-client-site-redesign-proof.md`, `docs/pilot-client-site-visual-direction.md`) declaring `type: artifact`, which is not in the current `type` enum (project/plan/decision/log/architecture/strategy/reference/continuation/proposal/template/review). None of this debt was created by the settlement being applied - it predates Agent Ask's adoption in that repo by weeks.\nROOT CAUSE, READ FROM ARCADIA'S OWN SOURCE (not inferred). Two things compound:\n1. `settle --apply`'s file-mutation path calls `syncProjectDocs` (src/docs/sync.ts:105), which calls `discoverDocs(repoRoot)` (src/docs/sync.ts:135) to crawl and validate every managed document in the entire repository, not only the documents the settling Ask touches. Any error anywhere throws `Accepted Agent Ask managed documents failed operational sync.` (src/ask/settlement.ts:482) and the settlement's writes are refused.\n2. The Agent Ask contract has no intent whose settlement corrects or amends an existing document. `arcadia agent-ask contract`'s intent table is entirely additive: `log` appends one entry (never amends a past one), `plan` and `action` create or amend Plan Actions specifically (not arbitrary document fields like a `type:` frontmatter value or a Log entry's body), `decision`/`artifact`/`project_update` create records, `outcome`/`milestone` update two named Project fields. Nothing targets \"this existing document has an error, fix it.\"\nCombined, this means: once (1) trips on some old, unrelated document, there is no intent under (2) that can ever clear it. The repository's very first attempted settlement hit this. Any other adopting project with any amount of historical drift will hit it the same way, on the first real use, for reasons unconnected to whatever they were actually trying to settle.\nWHY THIS BELONGS TO ARCADIA AND NOT TO THE ADOPTING PROJECT. Private Practice Now's operator's own instinct, on hearing about the stale MISSION_LOG entries and mistyped docs, was \"ask Arcadia to resolve it\" - not \"let the agent hand-edit it.\" That instinct is exactly right under AGENTS.md's own rule that coding agents change project files, never administration artifacts. But it currently has nowhere to go: I could not construct any Agent Ask, of any intent, that corrects a pre-existing MISSION_LOG entry's format or a doc's `type:` field. The capability the operator wants used does not exist yet.\nSUGGESTIONS FOR CONSIDERATION.\nA. Add a `correction` (or similarly named) intent: `target_ref` names the existing document or entry (e.g. `log/<repo>/<date>#<heading>`, `doc/<relative-path>#type`), and settlement rewrites just that document's offending field or entry to a compliant form, with the same propose-then-operator-settles boundary every other intent already has. This is the smallest change that gives \"ask Arcadia to resolve it\" a real target, and it reuses the existing settlement/authority machinery rather than adding a new one.\nB. Reconsider `syncProjectDocs`'s scope inside settlement. Validating only the documents the settling Ask actually wrote (plus documents that structurally reference them, e.g. a Plan's own Actions) would let a settlement succeed on its own merits even while unrelated historical debt sits unresolved elsewhere in the repo - closer to how a linter incremental mode treats a pre-existing violation outside the diff. Full-corpus validation could remain available as an explicit, separate `arcadia docs sync` health check rather than a silent gate on every unrelated write. This is optional if (A) ships, since (A) at least gives repos a way to pay down the debt Ask by Ask; without (A), (B) alone would still leave no way to fix the flagged documents themselves, only a way to stop them from blocking unrelated work.\nC. Whichever way this resolves, `arcadia agent-ask contract` should keep describing exactly what is and is not correctable, the same way it already states the authority boundary - so an agent learns the limits from the contract instead of from a failed settlement.\nNOT ATTEMPTED. I did not hand-edit Private Practice Now's MISSION_LOG.md or the four mistyped docs to route around this - that is precisely the boundary this finding is about. I also did not modify Arcadia's own source to add a correction intent; that is a real design decision (what is correctable, by whom, under what review) belonging to Arcadia's own governance, not something to improvise mid-task in an adopting project's session."
confidence: high
plan: way-delivery
updated: 2026-09-03
---

# Decision 0044: Decide whether Agent Ask should gain a capability to correct or amend an existing managed document, and whether settle's full-corpus revalidation on every apply should change. Right now neither exists, and together they mean that once any single managed document anywhere in a governed repo drifts out of schema, every future Agent Ask settlement in that repo is permanently blocked with no path through Arcadia to fix it.

Proposed by Agent Ask agent-ask-has-no-correction-capability-2026-09-03. This Decision remains open until the operator answers it.
