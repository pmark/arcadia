agent_ask: v1
request_id: complete-make-worktree-runtime-self-contained-native-profile-2026-09-07
project: arcadia
intent: complete
target_ref: action/make-worktree-runtime-self-contained
candidate_revision: f90527b219ea33cc0dcc1ff046763dd7947e1a91
desired_result: Record reviewable evidence that Arcadia Go uses the supported constrained Codex permission profile for CLI and has an explicit fail-closed Desktop/iPhone handoff.
requested_authority: apply_if_approved
evidence:
  - criterion: Run governed advance and work monitoring through the installed revision-pinned compiled broker; no required Arcadia Go lifecycle step depends on the tsx CLI IPC server.
    status: met
    note: Protected go, advance, and work-monitor brokers ran from the revision-pinned release; status reports release f90527b219ea33cc0dcc1ff046763dd7947e1a91 ready.
  - criterion: Resolve or avoid the observed macOS listen EPERM failure for required TypeScript-backed development commands without enabling unrestricted network access or danger-full-access, and retain a denial-focused diagnostic that names the blocked path or operation.
    status: met
    note: The installer uses the compiled broker runtime and profile network.enabled=false. Direct macOS profile probes completed candidate/temp writes; denial output identifies blocked .env file-write-create and keeps one recovery command in the managed skill.
  - criterion: Make dependency preparation idempotent and prove workspace package imports exercise the candidate worktree's source or candidate build, never the main checkout's stale dist; add a sentinel regression fixture that fails if resolution crosses back to the main checkout.
    status: met
    note: The real revision-pinned disposable host probe ran pnpm bridge:worktree and the existing sentinel regression suite tests/bridge-worktree-deps.test.ts passed.
  - criterion: From a prepared worktree under the intended Codex sandbox, prove ordinary source edits, node_modules preparation, dist and .next output, temporary SQLite databases, Vitest, build, and cleanup all work without an operator approval prompt.
    status: met
    note: The installer host probe ran the candidate build, Dashboard .next build, temporary SQLite, Vitest, cleanup, and compiled-broker check. A direct codex sandbox -P arcadia-unattended probe wrote and removed a candidate file plus a system-temp file without approval.
  - criterion: The installer performs a disposable post-install host probe covering candidate-root writes, dependency preparation, temporary files, and the compiled preflight; any failure leaves recoverable state and reports one exact remedy before Arcadia launches production work.
    status: met
    note: go-broker install completed the disposable host probe from the pinned f90527b release and go-broker status reports every agent setup check ready. The runtime probe names the blocked operation/path and emits its exact recovery command.
  - criterion: Keep the common path deterministic and local; diagnose a failed probe with one bounded model-bearing repair pass only after preserving the denial evidence.
    status: met
    note: Validation used deterministic TypeScript, Vitest, build, installer, status, and macOS sandbox commands. The only model-bearing repair was this bounded implementation pass after preserving the native app acceptance failure and sandbox denial evidence.
